# OpenCS Aalto.fi Web Development course
In this course, I learned about using Svelte and Deno to build a full-stack development app. I used Docker to package the code.
## Installation
npm install
### Run 
docker compose up --build

### Test
After run, open a separate terminal and run docker compose run --rm --entrypoint=npx e2e-tests playwright test